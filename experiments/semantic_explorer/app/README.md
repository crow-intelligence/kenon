# Semantic Explorer App

Open `index.html` in a browser after running the data prep scripts.

For `fetch()` to work, serve with a local HTTP server:

```bash
cd experiments/semantic_explorer/app
python -m http.server 8080
# Open http://localhost:8080
```
